export class ClientRegister {
  public firstName: string;
  public lastName: string;
  public plus: number;

  constructor(firstName: string, lastName: string, plus: number) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.plus = plus;
  }
}
